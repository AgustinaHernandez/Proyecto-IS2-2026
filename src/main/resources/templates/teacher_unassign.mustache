{{>header}}

<body class="bg-gray-100 dark:bg-gray-900 flex items-center justify-center min-h-screen p-4 transition-colors duration-300">
    
    <div class="bg-white dark:bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md border border-gray-200 dark:border-gray-700">
        <h1 class="text-3xl font-bold text-center text-gray-800 dark:text-white mb-2">Desasignar Profesor</h1>
        <p class="text-sm text-center text-gray-500 dark:text-gray-400 mb-6">Remueve la asignación de un docente de una materia específica.</p>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/teacher/unassign" method="post" class="space-y-6" onsubmit="return confirm('¿Seguro que deseas remover a este profesor de la materia elegida?');">
            
            <div>
                <label for="subject_id" class="block text-gray-700 dark:text-gray-300 text-sm font-medium mb-1">Materia:</label>
                <select id="subject_id" name="subject_id" required class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                    <option value="" disabled selected>Seleccione la materia</option>
                    {{#subjects}}
                    <option value="{{id}}">[{{code}}] {{name}}</option>
                    {{/subjects}}
                </select>
            </div>

            <div>
                <label for="teacher_id" class="block text-gray-700 dark:text-gray-300 text-sm font-medium mb-1">ID del Profesor a desasignar:</label>
                <input type="number" id="teacher_id" name="teacher_id" required placeholder="Ej: 3"
                       class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
            </div>

            <button type="submit"
                    class="w-full bg-orange-600 text-white py-2 px-4 rounded-md hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-orange-500 focus:ring-offset-2 transition-colors duration-200 mt-4">
                Confirmar Desasignación
            </button>
            
            <div class="text-center mt-4">
                <a href="/dashboard" class="text-blue-600 dark:text-blue-400 hover:underline text-sm">Volver al Dashboard</a>
            </div>
        </form>
    </div>

</body>
</html>