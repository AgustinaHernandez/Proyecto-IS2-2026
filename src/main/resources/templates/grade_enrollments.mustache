{{> header}}

<body class="bg-gray-100 dark:bg-gray-900 min-h-screen p-4 transition-colors duration-300 flex justify-center items-start pt-10">
    
    <div class="bg-white dark:bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-5xl border border-gray-200 dark:border-gray-700">

    <h1 class="text-3xl font-bold text-center text-gray-800 dark:text-white mb-6">Materias actuales: Año {{currentYear}}</h1>

        <div class="relative overflow-x-auto shadow-sm sm:rounded-lg border border-gray-200 dark:border-gray-700 mb-6">
            <table class="w-full text-sm text-left text-gray-700 dark:text-gray-300">
                <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                    <tr>
                        <th scope="col" class="px-6 py-3">Código</th>
                        <th scope="col" class="px-6 py-3">Nombre</th>
                        <th scope="col" class="px-6 py-3">Condición inicial</th>
                    </tr>
                </thead>
                
                <tbody>
                    {{#currSubjects}}
                    <tr class="bg-white border-b dark:border-gray-700 dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors duration-200">
                        <td class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                            {{code}}
                        </td>
                        <td class="px-6 py-4">{{name}}</td>
                        <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">{{initial_condition}}</td>
                    </tr>
                    {{/currSubjects}}
                    {{^currSubjects}}
                    <tr class="bg-white dark:bg-gray-800">
                        <td colspan="5" class="px-6 py-8 text-center text-gray-500 dark:text-gray-400">
                            No se encontraron materias actuales.
                        </td>
                    </tr>
                    {{/currSubjects}}
                </tbody>
            </table>
        </div>

        <h1 class="text-3xl font-bold text-center text-gray-800 dark:text-white mb-6">Materias cursadas anteriormente</h1>

        <div class="relative overflow-x-auto shadow-sm sm:rounded-lg border border-gray-200 dark:border-gray-700 mb-6">
            <table class="w-full text-sm text-left text-gray-700 dark:text-gray-300">
                <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                    <tr>
                        <th scope="col" class="px-6 py-3">Código</th>
                        <th scope="col" class="px-6 py-3">Nombre</th>
                        <th scope="col" class="px-6 py-3">Año</th>
                        <th scope="col" class="px-6 py-3">Condición inicial</th>
                        <th scope="col" class="px-6 py-3">Condición final</th>
                    </tr>
                </thead>
                
                <tbody>
                    {{#gradeSubjects}}
                    <tr class="bg-white border-b dark:border-gray-700 dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors duration-200">
                        <td class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                            {{code}}
                        </td>
                        <td class="px-6 py-4">{{name}}</td>
                        <td class="px-6 py-4">{{year}}</td>
                        <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">{{initial_condition}}</td>
                        <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">{{final_condition}}</td>
                    </tr>
                    {{/gradeSubjects}}
                    {{^gradeSubjects}}
                    <tr class="bg-white dark:bg-gray-800">
                        <td colspan="5" class="px-6 py-8 text-center text-gray-500 dark:text-gray-400">
                            No se encontraron materias cursadas anteriormente.
                        </td>
                    </tr>
                    {{/gradeSubjects}}
                </tbody>
            </table>
        </div>

        <div class="flex justify-center mt-6">
            <a href="/student/subjects" class="bg-blue-600 text-white py-2 px-6 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors duration-200 text-center shadow-sm">
                Volver a selección de carreras
            </a>
        </div>

    </div>
</body>
</html>